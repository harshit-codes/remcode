{
  "name": "remcode-local-test",
  "version": "1.0.0",
  "description": "Local test environment for remcode features",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["remcode", "test", "local"],
  "author": "harshit-codes",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.0.3"
  }
}
